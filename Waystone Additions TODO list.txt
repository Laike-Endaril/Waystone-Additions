Fix waystones being detected as unactivated by client after someone else renames them (Waystones core bug)

Fix waystones being renamable by non-owners when "only owner can rename" option is enabled (Waystones core bug)

Implement config options

Add renders for protected zones

Add a way to lay siege to a waystone (from outside the block protection zone)
